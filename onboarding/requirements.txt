# Onboarding module — Python dependencies
# Install with: pip install -r onboarding/requirements.txt

# ── Azure AI Search ──────────────────────────────────────────────────────────
azure-search-documents==11.6.0

# ── Azure OpenAI (embeddings) ────────────────────────────────────────────────
openai>=1.68.0

# ── Azure AI Document Intelligence (PDF parsing) ────────────────────────────
azure-ai-formrecognizer>=3.3.0

# ── Azure identity (for DefaultAzureCredential) ──────────────────────────────
azure-identity>=1.17.0

# ── Tokenisation — must match the pilot scripts ──────────────────────────────
tiktoken>=0.7.0

# ── FastAPI + server ─────────────────────────────────────────────────────────
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9   # required for FastAPI file uploads

# ── Environment variables ────────────────────────────────────────────────────
python-dotenv>=1.0.1

# ── PDF fallback (when Document Intelligence is not configured) ──────────────
pypdf>=4.0.0

# ── DOCX support ─────────────────────────────────────────────────────────────
python-docx>=1.1.0

# ── Pydantic (FastAPI dependency — pinned for compatibility) ─────────────────
pydantic>=2.0.0,<3.0.0
