# ── Azure AI Search ──────────────────────────────────────────────────────────
azure-search-documents==11.6.0

# ── Tokenisation (for chunk-size counting) ───────────────────────────────────
tiktoken>=0.7.0

# ── FastAPI + server (Day 2 — agent core) ────────────────────────────────────
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0

# ── Azure AI Foundry Agents SDK ──────────────────────────────────────────────
azure-ai-projects>=2.3.0
azure-identity>=1.17.0

# ── Azure AI Content Safety (Day 3 — content safety wiring) ─────────────────
azure-ai-contentsafety>=1.0.0

# ── OpenAI SDK used by the Foundry agent endpoint ───────────────────────────
openai>=1.68.0

# ── Environment variables ───────────────────────────────────────────────────
python-dotenv>=1.0.1

# ── PDF parsing (onboarding module) ─────────────────────────────────────────
pypdf>=4.0.0

# ── PostgreSQL / ORM / Migrations (Day 4+) ───────────────────────────────────
# Async SQLAlchemy 2.0 + asyncpg for all FastAPI async routes.
# psycopg2-binary is used only by Alembic env.py (sync) and seed scripts.
sqlalchemy>=2.0.0,<3.0.0
alembic>=1.13.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.0

# ── Microsoft Teams / Bot Framework ──────────────────────────────────────────
botbuilder-core
botbuilder-integration-aiohttp

# ── Authentication + RBAC ────────────────────────────────────────────────────
# argon2-cffi: password hashing. PyJWT: HttpOnly-cookie session tokens.
argon2-cffi>=23.1.0
PyJWT>=2.8.0
email-validator>=2.1.0

pypdf

# ── Test-only: async SQLite engine for auth/RBAC/persistence tests ──────────
aiosqlite>=0.20.0
